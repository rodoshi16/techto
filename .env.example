# Copy to .env in project root (never commit .env)

# Required for real Sage brain (get from console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# Required for Sora voice (get from elevenlabs.io → Profile → API Key)
ELEVENLABS_API_KEY=...

# Optional — default is Rachel (clear, friendly)
# Find IDs at https://api.elevenlabs.io/v1/voices
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Model (haiku = cheap/fast for hackathon)
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

PORT=3001
